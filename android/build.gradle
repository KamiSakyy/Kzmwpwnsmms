// Root build file. AniBeat — native Android port of the web client.
plugins {
    id 'com.android.application' version '8.5.2' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
